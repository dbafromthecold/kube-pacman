apiVersion: v1
kind: Service
metadata:
  name: kube-pacman
  namespace: kube-pacman
  labels:
    app.kubernetes.io/name: kube-pacman
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: kube-pacman
  ports:
    - name: http
      port: 80
      targetPort: http
